# Mixed Array String Compression

- [x] Record the Workshop element-count motivation for compressing large arrays.
- [x] Convert `CUTTING_RESULT` to the established `Mapped Array` plus `String Split` pattern without changing its final mixed value shape.
- [x] Convert `ADDITIONAL_MATERIAL_LIST` with the same pattern.
- [x] Restore nested `Array(...)` values at their original indices after numeric decoding.
- [x] Compare locale files before editing and reuse generated blocks when their decoded data is identical.
- [x] Apply the conversion to restaurant, cafe, GC, and N3 variants.
- [x] Leave `kc_kr.ow` unchanged at the user's request.
- [x] Verify every decoded entry, nested-array shape, initialization order, string-segment length, version string, and line ending.
- [ ] Run an in-game Workshop import/runtime test.
